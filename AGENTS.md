# 후속 개발 지침

- 이 저장소는 `ldjkorea/rental-app`의 로컬 개선본이다. 원본 기준은 README에 기록했다.
- 변경 전 계획·영향 범위·예상 결과를 한국어로 간단히 설명한다.
- 기존 기능과 JSON 백업 호환성을 유지하며 작은 단위로 개선한다.
- `assets/core.js`는 DOM 없는 데이터 검증·저장 기능, `assets/app.js`는 기존 전역 함수를 사용하는 UI·동기화 코드다.
- 프레임워크나 런타임 라이브러리 추가 전에 기존 JavaScript와 내장 기능을 우선 검토한다.
- `rentalApp.state.v1`이 현재 저장값이며 원본의 개별 저장 키는 호환·복구 목적으로 남아 있다. `localStorage.clear()`를 사용하지 않는다.
- 손상된 원본·최신 고지서·과거 금액을 조용히 덮어쓰지 않는다. 데이터 구조 변경은 마이그레이션과 복원 흐름을 함께 검토한다.
- 기존 Google Apps Script 주소로 실제 데이터를 쓰는 테스트를 하지 않는다. 독립 브라우저 프로필, 샘플 자료, 모의 응답을 사용한다.
- 구글 서버 코드는 이 저장소에 없다. 인증·저장 구조·기기 간 충돌 해결을 확인했다고 가정하지 않는다.
- 배분·납부·계약 규칙은 `docs/ROADMAP.md`의 운영 결정 사항을 확인한 뒤 수정한다.
- 금액 또는 저장 로직 변경은 `npm test`와 관련 Edge 시나리오를 검증한다. `npm run check`로 문법을 확인한다.
- UI 변경은 PC와 모바일에서 확인한다. 브라우저 테스트 방법은 `docs/VALIDATION.md`에 있다.
- 배포 시 앱 정적 파일을 함께 반영하고 `sw.js` 캐시 버전을 갱신한다.
- `임대관리_PWA.zip`은 원본 과거 자료다. 수정한 최신 소스라고 오인하지 않는다.
- 실제 운영 반영 결과와 로컬 검증 결과를 구분해 보고한다.

## 버전 1.2 후속 기준
- 사용자 확정: 전기 2·3층 공급가 35,000원+VAT, 4층 잔액. 수도 층별 1명.
- 엘리베이터·오물비는 실비 합계를 보존한다. 기본 청구액 입력과 공급가 선택을 구분한다.
- 계산·납부 상태는 assets/billing.js에서 통일하고 중복 계산을 만들지 않는다.
- 완납 날짜 변경은 변경 이력을 남기고 메시지와 대시보드에 함께 반영한다.
- 예시 모드의 rentalDemo. 저장소 격리와 외부 연결 차단을 유지한다.
